import { useQuery } from '@tanstack/react-query'
import { api } from '../../services/api'
import { PageHeader } from '../../components/common/PageHeader'
import { DataTable } from '../../components/ui/DataTable'
import { StatusBadge } from '../../components/ui/StatusBadge'
import { dateTime } from '../../utils/format'

export function SettingsPage(){const query=useQuery({queryKey:['settings'],queryFn:()=>api('/api/settings')});const data=query.data;return <><PageHeader eyebrow="Hệ thống" title="Cài đặt & thiết bị" description="Thông tin chi nhánh, tài khoản nhận tiền và trạng thái thiết bị."/><div className="section-grid"><section><div className="section-header"><div><h2>Chi nhánh</h2><p>Thông tin vận hành hiện tại</p></div></div><DataTable rows={data?.branches} loading={query.isLoading} columns={[{key:'name',label:'Chi nhánh',render:(r)=><div><span className="cell-primary">{r.name}</span><div className="cell-secondary">{r.code}</div></div>},{key:'address',label:'Địa chỉ'},{key:'status',label:'Trạng thái',render:(r)=><StatusBadge status={r.status}/>}]} /></section><section><div className="section-header"><div><h2>Tài khoản nhận tiền</h2><p>Public và private</p></div></div><DataTable rows={data?.bankAccounts} loading={query.isLoading} columns={[{key:'bank',label:'Ngân hàng',render:(r)=><div><span className="cell-primary">{r.bank}</span><div className="cell-secondary">{r.accountNumber}</div></div>},{key:'accountName',label:'Chủ tài khoản'},{key:'visibility',label:'Phạm vi'}]} /></section></div><section className="mt-7"><div className="section-header"><div><h2>Thiết bị</h2><p>DAH-1017 và cổng kiểm soát; tích hợp đồng bộ vẫn tạm hoãn.</p></div></div><DataTable rows={data?.devices} loading={query.isLoading} columns={[{key:'device',label:'Thiết bị',render:(r)=><div><span className="cell-primary">{r.name}</span><div className="cell-secondary">{r.code} · {r.model}</div></div>},{key:'branch',label:'Chi nhánh'},{key:'ip',label:'IP'},{key:'pendingJobs',label:'Chờ đồng bộ',className:'text-right'},{key:'errors24h',label:'Lỗi 24h',className:'text-right'},{key:'lastHeartbeat',label:'Heartbeat',render:(r)=>dateTime(r.lastHeartbeat)},{key:'status',label:'Trạng thái',render:(r)=><StatusBadge status={r.status}/>}]} /></section></>}
